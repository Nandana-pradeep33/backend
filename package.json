{
  "name": "spectroski",
  "version": "1.0.0",
  "main": "server/index.js",
  "license": "MIT",
  "scripts": {
    "start": "nodemon .",
    "dev": "nodemon .",
    "setup": "npm install && npm install --force && nodemon .",
    "heroku-postbuild": "cd client && npm install --force && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongodb": "^5.8.1",
    "mongoose": "^5.13.20",
    "nodemon": "^2.0.7"
    "cors": "^2.8.5"
  },
  "optionalDependencies": {
    "fsevents": "^2.3.2"
  },
  "engines": {
    "node": "16.x",
    "npm": "7.x"
  }
}
